// enum Role {
//   Admin, // 0
//   Editor, // 1
//   Guest, // 2
// }

let userRole: 'Admin' | 'Editor' | 'Guest' = 'Admin';

// ...

userRole = 'Guest';

let possibleValues: [1 | -1, 1 | -1];

possibleValues = [1, -1];
